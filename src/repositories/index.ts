import type{AppState,ChildProfile,LessonProgress}from'../app/types';export interface ProfileRepository{save(profile:ChildProfile):Promise<void>;get():Promise<ChildProfile|null>}export interface ProgressRepository{save(progress:LessonProgress):Promise<void>;getAll():Promise<LessonProgress[]>}export interface AppRepository extends ProfileRepository,ProgressRepository{loadState():Promise<AppState>}export class LocalRepository implements AppRepository{async save(p:ChildProfile){const s=JSON.parse(localStorage.getItem('pochemuchka_state_v1')||'{}');localStorage.setItem('pochemuchka_state_v1',JSON.stringify({...s,profile:p}))}async get(){const s=JSON.parse(localStorage.getItem('pochemuchka_state_v1')||'{}');return s.profile||null}async saveProgress(p:LessonProgress){const s=JSON.parse(localStorage.getItem('pochemuchka_state_v1')||'{}');localStorage.setItem('pochemuchka_state_v1',JSON.stringify({...s,progress:{...(s.progress||{}),[p.lessonId]:p}}))}async save(p:LessonProgress):Promise<void>{return this.saveProgress(p)}async getAll(){const s=JSON.parse(localStorage.getItem('pochemuchka_state_v1')||'{}');return Object.values(s.progress||{}) as LessonProgress[]}async loadState(){const profile=await this.get(),progress=await this.getAll();return{profile,progress:Object.fromEntries(progress.map(p=>[p.lessonId,p])),xp:progress.reduce((n,p)=>n+p.xp,0),stars:progress.reduce((n,p)=>n+p.stars,0)}}}