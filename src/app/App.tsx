import{useEffect,useState}from'react';import type{ReactNode}from'react';import type{AppState,ChildProfile,Lesson,LeaderboardEntry}from'./types';import{storage,learning,audio,telegram,ageAdaptation}from'../services/core';import{backend}from'../services/backend';import{lessons,worlds,topics}from'../content/content';import{Mascot}from'../components/Mascot';import{QuizActivity,DragDropActivity,MatchingActivity}from'../features/activities';
function Shell({children,state,goHome}:{children:ReactNode;state:AppState;goHome:()=>void}){return <main className="app"><header className="top"><button className="brand" onClick={goHome}><Mascot/><span><b>ПОЧЕМУЧКА</b><small>Узнавай. Играй. Открывай мир!</small></span></button><div className="header-actions"><button className="icon-btn" onClick={()=>{audio.toggle();storage.save(state);location.reload()}} aria-label="Звук">{audio.enabled?'🔊':'🔇'}</button></div></header>{children}</main>}
function Profile({onDone}:{onDone:(p:ChildProfile)=>void}){const[n,setN]=useState(telegram.firstName());const[a,setA]=useState(7);function save(){if(!n.trim())return;const now=new Date().toISOString();onDone({id:crypto.randomUUID(),name:n.trim(),age:a,avatar:'🐱',createdAt:now,updatedAt:now})}return <section className="screen center"><div className="hero"><Mascot mood="happy"/><h1>Привет! Я Кот-Почемучка</h1><p>Будем узнавать новое, играть и открывать мир.</p></div><label>Как тебя зовут?<input value={n} onChange={e=>setN(e.target.value.slice(0,20))} placeholder="Например, Амина" autoFocus/></label><label>Сколько тебе лет?</label><div className="chips">{[6,7,8,9,10].map(x=><button className={a===x?'chip active':'chip'} key={x} onClick={()=>setA(x)}>{x}</button>)}</div><button className="primary" disabled={!n.trim()} onClick={save}>Начать приключение →</button></section>}
function Home({s,nav}:{s:AppState;nav:(r:string)=>void}){const age=s.profile!.age;const due=Object.values(s.progress).find(p=>p.nextReviewAt&&new Date(p.nextReviewAt)<=new Date()); const next=s.lastLessonId&&s.progress[s.lastLessonId]?.status==='in_progress'?lessons.find(l=>l.id===s.lastLessonId):due?lessons.find(l=>l.id===due.lessonId):lessons.find(l=>l.ageMin<=age&&l.ageMax>=age&&!s.progress[l.id]);return <section><div className="welcome card"><div><h1>Привет, {s.profile!.name}! 👋</h1><p>Готов узнать что-нибудь новое?</p></div><div className="big-stat">🏆 {s.xp}<small>Баллы</small></div></div><button className={next?'continue card':'continue card complete'} disabled={!next} onClick={()=>next&&nav('lesson:'+next.id)}><span>{next?'▶️':'🎉'}</span><div><b>{next?'Продолжить обучение':'На сегодня всё сделано!'}</b><small>{next?.title||'Все доступные уроки пройдены. Молодец!'}</small></div>{next&&<strong>→</strong>}</button><h2>Твои миры</h2><div className="world-grid">{worlds.filter(w=>w.isActive).map(w=>{const ls=lessons.filter(l=>l.worldId===w.id&&l.ageMin<=age&&l.ageMax>=age),done=ls.filter(l=>s.progress[l.id]?.status==='completed').length;return <button className="world-card" key={w.id} onClick={()=>nav('world:'+w.id)}><span>{w.icon}</span><b>{w.title}</b><small>{done}/{ls.length} уроков</small><i><em style={{width:(ls.length?done/ls.length*100:0)+'%'}}/></i></button>})}</div><div className="actions"><button onClick={()=>nav('progress')}>📈 Мой прогресс</button><button onClick={()=>nav('leaderboard')}>🏆 Рейтинг</button><button onClick={()=>nav('achievements')}>🏅 Достижения</button><button onClick={()=>nav('parent')}>👨‍👩‍👧 Родителям</button></div></section>}
function World({s,id,nav}:{s:AppState;id:string;nav:(r:string)=>void}){const w=worlds.find(x=>x.id===id)!;const age=s.profile!.age;const ls=lessons.filter(l=>l.worldId===id&&l.ageMin<=age&&l.ageMax>=age).sort((a,b)=>a.orderIndex-b.orderIndex);return <section><button className="back" onClick={()=>nav('home')}>← Назад</button><div className="world-head card"><span>{w.icon}</span><div><h1>{w.title}</h1><p>{w.description}</p></div></div>{topics.filter(t=>t.worldId===id).map(t=><div key={t.id}><h2>{t.title}</h2><p className="muted">{t.description}</p></div>)}<div className="lesson-list">{ls.map(l=>{const st=learning.status(s,l);const retryLocked=st==='completed_locked';return <button className={'lesson-row '+st} disabled={st==='locked'||retryLocked} key={l.id} onClick={()=>nav('lesson:'+l.id)}><span>{s.progress[l.id]?.status==='completed'?'⭐':retryLocked?'⏳':st==='locked'?'🔒':st==='in_progress'?'▶️':'○'}</span><div><b>{l.title}</b><small>{retryLocked?'Повторить можно завтра':l.description}</small></div><strong>{s.progress[l.id]?.status==='completed'?(retryLocked?'Завтра':'Пройдено'):st==='locked'?'Закрыт':st==='in_progress'?'Продолжить →':'Начать →'}</strong></button>})}</div></section>}
function Lesson({s,lesson,done,back,sync}:{s:AppState;lesson:Lesson;done:(s:AppState)=>void;back:()=>void;sync:(s:AppState)=>void}){const[started]=useState(()=>learning.start(s,lesson.id));const[step,setStep]=useState(0);useEffect(()=>{sync(started)},[started,sync]);const[score,setScore]=useState(0);const act=lesson.steps[step];const timerSeconds=ageAdaptation.timerSeconds(s.profile!.age);useEffect(()=>{void backend.saveProgress(started.progress[lesson.id]);void backend.event('lesson_started',{lessonId:lesson.id})},[started,lesson.id]);function result(ok:boolean){const ns=score+(ok?1:0);setScore(ns);if(step<lesson.steps.length-1){setStep(x=>x+1);return}const current=storage.load();let next:AppState;try{next=learning.complete(current,lesson,ns)}catch{next=storage.load();const fallback=current.progress[lesson.id];if(fallback){next.progress[lesson.id]={...fallback,status:'completed',score:ns,mastery:Math.max(0,Math.min(1,ns/Math.max(1,lesson.steps.length))),attempts:(fallback.attempts||0)+1,completedAt:new Date().toISOString()};storage.save(next)}}done(next);window.setTimeout(()=>{void backend.saveProgress(next.progress[lesson.id]).catch(()=>{});void backend.event('lesson_completed',{lessonId:lesson.id,score:ns,total:lesson.steps.length,mastery:next.progress[lesson.id]?.mastery??0})},0)}function onAttempt(ok:boolean,answer:unknown,timeSpent?:number){try{const n=storage.activityMastery(act.id,ok);void backend.saveActivityAttempt({profileId:n.profile!.id,activityId:act.id,isCorrect:ok,answer,timeSpent}).catch(()=>{})}catch{}}return <section><button className="back" onClick={back}>← Назад</button><div className="lesson-head card"><small>Шаг {step+1} из {lesson.steps.length}</small><h1>{lesson.title}</h1><p>{lesson.description}</p><div className="progress"><i style={{width:(step/lesson.steps.length*100)+'%'}}/></div></div><div className="card activity-card">{act.type==='quiz'?<QuizActivity key={act.id} activity={act} timerSeconds={timerSeconds} onResult={result} onAttempt={onAttempt}/>:act.type==='drag_drop'?<DragDropActivity key={act.id} activity={act} onResult={result} onAttempt={onAttempt}/>:<MatchingActivity key={act.id} activity={act} onResult={result} onAttempt={onAttempt}/>}</div></section>}
function Leaderboard({s,back}:{s:AppState;back:()=>void}){const[rows,setRows]=useState<LeaderboardEntry[]>([]);const[loading,setLoading]=useState(true);const[open,setOpen]=useState<string|null>(null);useEffect(()=>{backend.getLeaderboard().then(setRows).catch(()=>setRows([])).finally(()=>setLoading(false))},[]);return <section><button className="back" onClick={back}>← Назад</button><div className="card"><h1>🏆 Рейтинг</h1><p className="muted">Общий рейтинг по баллам за пройденные уроки.</p>{loading?<p>Загружаем рейтинг…</p>:rows.length===0?<p>Рейтинг пока пуст. Пройди первый урок!</p>:<div className="leaderboard">{rows.map(r=><div className={'leader-card '+(r.isCurrentUser?'current':'')} key={r.profileId}><button className="leader-row" type="button" onClick={()=>setOpen(open===r.profileId?null:r.profileId)}><strong>{r.rank}</strong><span className="leader-avatar-wrap"><span className="leader-avatar">{r.avatar}</span><small>{r.completedLessons}/{r.totalLessons}</small></span><div><b>{r.name}</b><small>Участник рейтинга</small></div><em>🏆 {r.points}</em></button>{open===r.profileId&&<div className="leader-lessons">{r.lessons.map(lesson=><div key={lesson.lessonId} className={lesson.completed?'lesson-done':'lesson-pending'}><span>{lesson.completed?'✓':'○'}</span><span>{lesson.title}</span></div>)}</div>}</div>)}</div>}</div></section>}function Progress({s,back}:{s:AppState;back:()=>void}){const[tab,setTab]=useState<'rating'|'stars'|'lessons'|null>(null);const[rows,setRows]=useState<LeaderboardEntry[]>([]);const[loading,setLoading]=useState(false);const age=s.profile!.age;const availableLessons=lessons.filter(l=>l.ageMin<=age&&l.ageMax>=age).sort((a,b)=>a.orderIndex-b.orderIndex);const completedLessons=availableLessons.filter(l=>s.progress[l.id]?.status==='completed');const weak=s.progress?Object.values(s.progress).filter(p=>p.mastery<.7&&p.attempts>0).map(p=>lessons.find(l=>l.id===p.lessonId)).filter(Boolean):[];function showRating(){setTab('rating');if(!rows.length){setLoading(true);backend.getLeaderboard().then(setRows).catch(()=>setRows([])).finally(()=>setLoading(false))}}const gifts=[{id:'sticker',icon:'🎁',title:'Набор наклеек',cost:5},{id:'avatar',icon:'🧢',title:'Новый аватар',cost:10},{id:'treasure',icon:'🪄',title:'Волшебный сундук',cost:15}];const[claimed,setClaimed]=useState<string[]>([]);return <section><button className="back" onClick={back}>← Назад</button><div className="stats-grid progress-cards"><button className={tab==='rating'?'stat-card active':'stat-card'} onClick={showRating}>🏆<b>{s.xp}</b><small>Баллы</small></button><button className={tab==='stars'?'stat-card active':'stat-card'} onClick={()=>setTab('stars')}>⭐<b>{s.stars}</b><small>Звёзды</small></button><button className={tab==='lessons'?'stat-card active':'stat-card'} onClick={()=>setTab('lessons')}>📚<b>{completedLessons.length}/{availableLessons.length}</b><small>Уроков</small></button></div><div className="card progress-content">{tab===null&&<><h2>Твой прогресс</h2><p>Нажми на любой показатель выше, чтобы посмотреть подробности.</p>{weak.length>0&&<><h3>🔁 Повторение</h3>{weak.map(l=><p key={l!.id}>• {l!.title} {s.progress[l!.id]?.nextReviewAt&&new Date(s.progress[l!.id].nextReviewAt!)<=new Date()?'— уже пора повторить':''}</p>)}</>}</>}{tab==='rating'&&<><h2>🏆 Рейтинг</h2><p className="muted">Твоё место выделено.</p>{loading?<p>Загружаем рейтинг…</p>:rows.length===0?<p>Рейтинг пока пуст.</p>:<div className="leaderboard">{rows.map(r=><div className={'leader-row '+(r.isCurrentUser?'current':'')} key={r.profileId}><strong>{r.rank}</strong><span className="leader-avatar">{r.avatar}</span><div><b>{r.name}</b><small>{r.isCurrentUser?'Это ты':'Участник'}</small></div><em>🏆 {r.points}</em></div>)}</div>}</>}{tab==='stars'&&<><h2>⭐ Подарки</h2><p className="muted">Обменивай звёзды на награды.</p><div className="gift-grid">{gifts.map(g=><div className="gift-card" key={g.id}><span>{g.icon}</span><b>{g.title}</b><small>{g.cost} ⭐</small><button disabled={claimed.includes(g.id)||s.stars<g.cost} onClick={()=>setClaimed(x=>[...x,g.id])}>{claimed.includes(g.id)?'Получено':s.stars>=g.cost?'Обменять':'Не хватает звёзд'}</button></div>)}</div></>}{tab==='lessons'&&<><h2>📚 Уроки</h2><p className="muted">Пройденные и ещё не пройденные уроки.</p><div className="lesson-progress-list">{availableLessons.map(l=>{const done=s.progress[l.id]?.status==='completed';return <div className={done?'lesson-progress-item done':'lesson-progress-item'} key={l.id}><span>{done?'⭐':'○'}</span><div><b>{l.title}</b><small>{done?'Пройден':'Не пройден'}</small></div></div>})}</div></>}</div></section>}

function Achievements({s,back}:{s:AppState;back:()=>void}){const completed=Object.values(s.progress).filter(p=>p.status==='completed').length;const achievements=[{id:'first-lesson',icon:'🎯',title:'Первый урок',text:'Пройди свой первый урок',done:completed>=1},{id:'five-lessons',icon:'📚',title:'5 уроков',text:'Пройди 5 уроков',done:completed>=5},{id:'hundred-points',icon:'🏆',title:'100 баллов',text:'Набери 100 баллов',done:s.xp>=100},{id:'ten-stars',icon:'⭐',title:'10 звёзд',text:'Получи 10 звёзд',done:s.stars>=10},{id:'first-language',icon:'🔤',title:'Первый язык',text:'Пройди первый урок из мира «Языки»',done:Object.values(s.progress).some(p=>p.status==='completed'&&lessons.find(l=>l.id===p.lessonId)?.worldId==='languages')},{id:'world-explorer',icon:'🌍',title:'Исследователь мира',text:'Пройди 3 урока из мира «Мир»',done:Object.values(s.progress).filter(p=>p.status==='completed'&&lessons.find(l=>l.id===p.lessonId)?.worldId==='world').length>=3},{id:'mathematician',icon:'🔢',title:'Математик',text:'Пройди 4 урока из мира «Математика»',done:Object.values(s.progress).filter(p=>p.status==='completed'&&lessons.find(l=>l.id===p.lessonId)?.worldId==='math').length>=4},{id:'animal-friend',icon:'🐾',title:'Друг животных',text:'Пройди 3 урока из мира «Животные»',done:Object.values(s.progress).filter(p=>p.status==='completed'&&lessons.find(l=>l.id===p.lessonId)?.worldId==='animals').length>=3}];return <section><button className="back" onClick={back}>← Назад</button><div className="card achievements-head"><h1>🏅 Достижения</h1><p>Собирай достижения, проходя уроки и открывая новые миры.</p><div className="achievement-count">{achievements.filter(a=>a.done).length} / {achievements.length} открыто</div></div><div className="achievement-grid">{achievements.map(a=><div className={a.done?'achievement-card unlocked':'achievement-card'} key={a.id}><span>{a.icon}</span><div><b>{a.title}</b><small>{a.text}</small></div>{a.done&&<em>✓</em>}</div>)}</div></section>}

function Parent({s,back}:{s:AppState;back:()=>void}){const[open,setOpen]=useState(false);const[v,setV]=useState('');return <section><button className="back" onClick={back}>← Назад</button>{!open?<div className="screen center"><h1>Для родителей</h1><p>Решите пример, чтобы открыть раздел.</p><p><b>7 + 5 = ?</b></p><input inputMode="numeric" value={v} onChange={e=>setV(e.target.value)}/><button className="primary" onClick={()=>v==='12'&&setOpen(true)}>Открыть</button></div>:<div className="card"><h1>Прогресс ребёнка</h1><div className="stats-grid"><div>⏱<b>—</b><small>Время</small></div><div>📚<b>{Object.values(s.progress).filter(p=>p.status==='completed').length}</b><small>Уроков</small></div><div>🏆<b>{s.xp}</b><small>Баллы</small></div></div><h2>Темы для повторения</h2>{Object.values(s.progress).filter(p=>p.mastery<.7).map(p=><p key={p.id}>🔁 {lessons.find(l=>l.id===p.lessonId)?.title}</p>)}</div>}</section>}
export function App(){const[s,setS]=useState<AppState>(storage.load);const[route,setRoute]=useState(location.hash.replace('#/','')||'home');useEffect(()=>{const f=()=>setRoute(location.hash.replace('#/','')||'home');addEventListener('hashchange',f);return()=>removeEventListener('hashchange',f)},[]);useEffect(()=>{if(!backend.enabled)return;let alive=true;const local=storage.load();backend.bootstrap().then(remote=>{if(!alive)return;if(!remote?.profile){if(local.profile){storage.save(local);setS(local);void backend.saveProfile(local.profile).catch(()=>{})}return}const rank=(p:any)=>p?.status==='completed'?2:p?.status==='in_progress'?1:0;const mergedProgress:any={...remote.progress};for(const lessonId of Object.keys(local.progress||{})){const lp:any=local.progress[lessonId];const rp:any=mergedProgress[lessonId];if(!rp||rank(lp)>rank(rp)||(rank(lp)===rank(rp)&&Number(lp.attempts||0)>Number(rp.attempts||0))){mergedProgress[lessonId]={...lp,profileId:remote.profile.id}}}const merged={...remote,progress:mergedProgress,activityMastery:{...remote.activityMastery,...local.activityMastery},lastLessonId:local.lastLessonId||remote.lastLessonId};storage.save(merged);setS(merged)}).catch(()=>{});return()=>{alive=false}},[]);const nav=(r:string)=>{location.hash='/'+r;setRoute(r)};const profileDone=(p:ChildProfile)=>{const n=storage.profile(p);setS(n);void backend.saveProfile(p);void backend.event('profile_created',{age:p.age});nav('home')};if(!s.profile)return <main className="app"><Profile onDone={profileDone}/></main>;let body:React.ReactNode;if(route==='home')body=<Home s={s} nav={nav}/>;else if(route.startsWith('world:'))body=<World s={s} id={route.slice(6)} nav={nav}/>;else if(route.startsWith('lesson:')){const l=lessons.find(x=>x.id===route.slice(7));body=l?<Lesson s={s} lesson={l} back={()=>nav('home')} sync={setS} done={x=>{setS(x);nav('home')}}/>:null}else if(route==='progress')body=<Progress s={s} back={()=>nav('home')}/>;else if(route==='leaderboard')body=<Leaderboard s={s} back={()=>nav('home')}/>;else if(route==='achievements')body=<Achievements s={s} back={()=>nav('home')}/>;else if(route==='parent')body=<Parent s={s} back={()=>nav('home')}/>;else body=<Home s={s} nav={nav}/>;return <Shell state={s} goHome={()=>nav('home')}>{body}</Shell>}
