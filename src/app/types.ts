export type ActivityType='quiz'|'drag_drop'|'matching'|'sorting'|'memory'|'find_object'|'sequence';export type LessonStatus='locked'|'available'|'in_progress'|'completed';export interface ChildProfile{id:string;name:string;age:number;avatar:string;createdAt:string;updatedAt:string}export interface Activity{id:string;type:ActivityType;title:string;instructions:string;data:unknown;reward?:{xp?:number;stars?:number}}export interface Lesson{id:string;worldId:string;topicId:string;title:string;description:string;ageMin:number;ageMax:number;difficulty:1|2|3;learningGoal:string[];orderIndex:number;steps:Activity[]}export interface Topic{id:string;worldId:string;title:string;description:string;orderIndex:number}export interface World{id:string;title:string;description:string;icon:string;orderIndex:number;isActive:boolean}export interface LessonProgress{id:string;profileId:string;lessonId:string;status:LessonStatus;score:number;mastery:number;attempts:number;startedAt?:string;completedAt?:string;nextReviewAt?:string;xp:number;stars:number}export interface ActivityAttempt{id:string;profileId:string;activityId:string;isCorrect:boolean;answer?:unknown;timeSpent?:number;createdAt:string}
export interface LeaderboardLesson{lessonId:string;title:string;completed:boolean}export interface LeaderboardEntry{rank:number;profileId:string;name:string;avatar:string;points:number;completedLessons:number;totalLessons:number;lessons:LeaderboardLesson[];isCurrentUser?:boolean}export interface AppState{profile:ChildProfile|null;progress:Record<string,LessonProgress>;activityMastery:Record<string,number>;xp:number;stars:number;purchasedGifts:string[];lastLessonId?:string}