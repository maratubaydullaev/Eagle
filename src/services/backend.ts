import type { AppState, ChildProfile, LessonProgress } from '../app/types'
import { telegram } from './core'
const url = import.meta.env.VITE_SUPABASE_URL as string | undefined
const configured = Boolean(url && telegram.initData)
const endpoint = url ? `${url.replace(/\/$/, '')}/functions/v1/pochemuchka-api` : ''
async function call(action: string, payload: Record<string, unknown> = {}) { if (!configured) return null; const res = await fetch(endpoint, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ initData: telegram.initData, action, payload }) }); const body = await res.json().catch(() => ({})); if (!res.ok || body.error) throw new Error(body.error || `backend ${res.status}`); return body }
function fromRow(row: any): LessonProgress { return { id: row.id, profileId: row.profile_id, lessonId: row.lesson_id, status: row.status, score: row.score ?? 0, mastery: Number(row.mastery ?? 0), attempts: row.attempts ?? 0, startedAt: row.started_at ?? undefined, completedAt: row.completed_at ?? undefined, xp: row.xp ?? 0, stars: row.stars ?? 0 } }
export const backend = { get enabled() { return configured }, async bootstrap(): Promise<AppState | null> { const body = await call('bootstrap'); if (!body) return null; const profile = body.profile ? { id: body.profile.id, name: body.profile.name, age: body.profile.age, avatar: body.profile.avatar || '🐱', createdAt: body.profile.created_at, updatedAt: body.profile.updated_at } as ChildProfile : null; const progress = Object.fromEntries((body.progress || []).map((r: any) => [r.lesson_id, fromRow(r)])); const ps = Object.values(progress) as LessonProgress[]; return { profile, progress, xp: ps.reduce((n, p) => n + p.xp, 0), stars: ps.reduce((n, p) => n + p.stars, 0), lastLessonId: ps.find(p => p.status === 'in_progress')?.lessonId } }, async saveProfile(profile: ChildProfile) { await call('save_profile', { name: profile.name, age: profile.age, avatar: profile.avatar }) }, async getDueReviews(): Promise<LessonProgress[]> { const body = await call('due_reviews'); if (!body) return []; return (body.progress || []).map(fromRow) }, async saveProgress(progress: LessonProgress) { await call('save_progress', { lessonId: progress.lessonId, status: progress.status, score: progress.score, mastery: progress.mastery, attempts: progress.attempts, startedAt: progress.startedAt, completedAt: progress.completedAt, xp: progress.xp, stars: progress.stars }) }, async event(eventName: string, payload: Record<string, unknown> = {}) { try { await call('analytics', { eventName, payload }) } catch { /* analytics must never block gameplay */ } } }
